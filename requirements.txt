# Core dependencies for the Artificer ML pipeline.
# Verified on Python 3.13 (Windows) inside the project-local .venv/.
#
# GPU setup (NVIDIA Blackwell / RTX 50-series need CUDA >= 12.8):
#   The default `torch` on PyPI is CPU-only. To train on the GPU, install a CUDA
#   12.8 build INTO your venv *before* installing the rest of these deps:
#       python -m venv .venv
#       .venv\Scripts\python -m pip install torch --index-url https://download.pytorch.org/whl/cu128
#       .venv\Scripts\python -m pip install -r requirements.txt
#   (Verified with torch 2.11.0+cu128.)
#
# Note: scripts import `datasets` before `torch` on purpose — importing torch
# first triggers a Windows DLL load-order crash with the CUDA build.

torch>=2.11
torchvision>=0.26
numpy==2.4.2
pandas==3.0.0
Pillow==12.3.0
transformers==5.12.1
datasets==5.0.0
matplotlib==3.11.0
